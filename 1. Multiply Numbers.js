function multiplyNumbers(arr) {
    let x=parseFloat(arr[0]);
    let y=parseFloat(arr[1]);

    return x*y;
}

var array=['3','2'];
console.log(multiplyNumbers(array));

array=['23632.36','-12.3249'];
console.log(multiplyNumbers(array));

